"""Standalone LEGO blocks used by the Webull chain.

Every ``step_*.py`` module is deliberately self-contained: it imports no
project module, contains its own goal and quick-start command, and can be run
directly as one Python file.
"""
