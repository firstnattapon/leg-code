# Copy these sections into Streamlit Community Cloud > App settings > Secrets.
# Never rename this file to secrets.toml and commit real credentials.

[firebase_service_account]
type = "service_account"
project_id = "YOUR_GCP_PROJECT_ID"
private_key_id = "YOUR_PRIVATE_KEY_ID"
private_key = "-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n"
client_email = "YOUR_SERVICE_ACCOUNT@YOUR_GCP_PROJECT_ID.iam.gserviceaccount.com"
client_id = "YOUR_CLIENT_ID"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "YOUR_CLIENT_CERT_URL"
universe_domain = "googleapis.com"

[lego_dashboard]
rows_collection = "webull_lego_rows"
state_collection = "webull_lego_state"
order_audit_collection = "webull_lego_order_audit"
fix_c = 1500.0
diff = 30.0
decimal_precision = 5
# Step 18 needs write permission on rows/state. Order audit falls back to the
# session if this optional write is disabled or denied.
audit_to_firestore = true
